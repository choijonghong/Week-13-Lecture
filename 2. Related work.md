# **2\. Related work**

GAN은 기존 생성 모델들이 가졌던 고질적인 문제들을 해결하며 등장했습니다. 업로드하신 문서를 바탕으로 각 모델의 특징과 한계, 그리고 GAN이 이를 어떻게 극복했는지 정리했습니다.

## **1\. 기존 생성 모델들의 주요 한계점**

### **🧩 RBM / DBM (비지도 생성 모델)**

* **핵심 문제**: **정규화 상수(Partition Function)** 계산의 어려움.  
* **기술적 한계**: 수만 차원의 데이터에서 이 값을 계산하는 것이 거의 불가능하며, 이를 근사하기 위한 **MCMC(마르코프 체인 몬테카를로)** 기법은 속도가 매우 느리고 불안정합니다.  
* **비유**: 방 전체를 탐색해야 하는 로봇이 한곳에서만 맴도는 'Mixing 문제'가 발생하여 학습 효율이 떨어집니다.

### **🧩 DBN (Deep Belief Network)**

* **구조적 한계**: 비지도 모델과 지도 모델이 섞인 **복잡한 하이브리드 구조**.  
* **학습 난이도**: 여러 단계를 순차적으로 학습시켜야 하므로 계산량이 방대하고 구현이 까다롭습니다.

### **🧩 NCE (Noise Contrastive Estimation)**

* **전략**: 진짜 데이터와 고정된 노이즈 데이터를 구별하며 학습.  
* **결정적 단점**: **노이즈 분포가 고정**되어 있어, 생성 모델의 성능이 조금만 좋아져도 노이즈를 너무 쉽게 구별하게 되어 학습이 정체(Stagnation)됩니다.

### **🧩 Autoencoder 계열 (AE / DAE)**

* **특징**: 데이터를 복원하는 방식으로 학습.  
* **품질 문제**: 결과물이 **흐릿하게(Blurry)** 재구성되는 경향이 있으며, 샘플링 시 마르코프 체인이 필요한 경우가 많아 속도가 느립니다.

### **🧩 VAE (Variational Autoencoder)**

* **당시 한계**: 2014년 당시 초기 모델들은 **KL 발산(KL Divergence)** 최적화 과정에서 데이터의 평균치를 생성하려다 보니, 이미지가 선명하지 못하고 부드럽게 뭉개지는 문제가 있었습니다.

## **2\. GAN의 혁신과 차별점 (The GAN Breakthrough)**

GAN은 위 모델들의 약점을 \*\*"경쟁적 학습"\*\*이라는 완전히 새로운 패러다임으로 해결했습니다.

| 비교 항목 | 기존 모델 (RBM, VAE 등) | GAN (Generative Adversarial Network) |
| :---- | :---- | :---- |
| **계산 방식** | 복잡한 확률 분포 $p(x)$ 직접 계산 | 확률 분포 계산 필요 없음 |
| **샘플링 기법** | MCMC 등 느린 반복 알고리즘 필요 | 한 번의 계산(Pass)으로 즉시 생성 |
| **학습 구조** | 정적 환경 혹은 고정된 대조군 | \*\*생성자(G) vs 판별자(D)\*\*의 동적 경쟁 |
| **결과물 품질** | 흐릿하거나 평균적인 이미지 | **선명하고(Sharp) 사실적인** 이미지 |

## **3\. 결론: GAN이 제시한 새로운 방향**

GAN은 "판별자를 속여라"라는 단순하고 강력한 목적 함수를 통해 기존 모델들이 해결하지 못한 세 가지를 동시에 달성했습니다.

1. **효율성**: MCMC와 같은 복잡한 샘플링 과정 제거.  
2. **지속성**: 라이벌(D)이 함께 강해지므로 학습 정체 현상을 극복.  
3. **선명도**: 픽셀 단위의 선명함이 살아있는 고품질 데이터 생성.

결과적으로 GAN은 생성 모델 연구의 흐름을 '복원'과 '확률 계산'에서 \*\*'사실적인 샘플 생성'\*\*으로 완전히 뒤바꾼 획기적인 전환점이 되었습니다.
# **생성적 적대 신경망 (GAN) 핵심 요약**

생성적 적대 신경망(GAN)은 이안 굿펠로우(Ian Goodfellow)가 제안한 모델로, 두 개의 신경망이 서로 경쟁하며 데이터를 생성하는 혁신적인 구조를 가지고 있습니다.

## **1\. GAN의 기본 구조와 비유**

GAN은 크게 두 가지 구성 요소로 나뉩니다.

* **생성자 (Generator,** $G$**):** 위조지폐범에 비유됩니다. 실제 데이터와 구분할 수 없는 가짜 데이터를 만들어내어 판별자를 속이는 것이 목표입니다.  
* **판별자 (Discriminator,** $D$**):** 경찰이나 감별사에 비유됩니다. 입력받은 데이터가 실제 데이터($x$)인지 생성자가 만든 가짜 데이터($G(z)$)인지를 정확히 구별해내는 것이 목표입니다.

이 두 네트워크는 \*\*"미니맥스 게임(Minimax Game)"\*\*을 통해 서로의 성능을 극적으로 향상시킵니다.

## **2\. 목표 함수 (Objective Function) 해설**

GAN의 학습은 다음의 가치 함수(Value Function) $V(D, G)$를 최적화하는 과정입니다.

$$\\min\_G \\max\_D V(D, G) \= \\mathbb{E}\_{x \\sim p\_{data}(x)}\[\\log D(x)\] \+ \\mathbb{E}\_{z \\sim p\_z(z)}\[\\log(1 \- D(G(z)))\]$$

### **항별 상세 설명**

1. **진짜 데이터 항:** $\\mathbb{E}\_{x \\sim p\_{data}(x)}\[\\log D(x)\]$  
   * 실제 데이터($x$)를 판별자($D$)에게 주었을 때, $D(x)$가 1(진짜)에 가까워지도록 학습합니다.  
   * 판별자($D$) 입장에서는 이 값을 **최대화**하여 진짜를 진짜라고 잘 맞혀야 합니다.  
2. **가짜 데이터 항:** $\\mathbb{E}\_{z \\sim p\_z(z)}\[\\log(1 \- D(G(z)))\]$  
   * 생성자가 만든 가짜 데이터($G(z)$)를 판별자에게 주었을 때, 판별자가 이를 0(가짜)으로 판단하게 만듭니다. 즉, $D(G(z))$를 0으로 만들어 $\\log(1-0) \= \\log(1) \= 0$이 되도록 합니다.  
   * \*\*판별자($D$)\*\*는 이 전체 값을 **최대화**하여 가짜를 잘 잡아내려 합니다.  
   * \*\*생성자($G$)\*\*는 이 전체 값을 **최소화**하려 합니다. 즉, $D(G(z))$를 1에 가깝게 만들어 판별자를 속이려 합니다.

## **3\. 학습 과정의 핵심 포인트**

### **초기 학습 문제 (Gradient Starvation)**

학습 초기에는 생성자가 만드는 데이터가 매우 조잡합니다. 이때 판별자는 가짜를 너무 쉽게 잡아내어 $D(G(z)) \\approx 0$이 됩니다. 이 경우 $\\log(1 \- D(G(z)))$의 기울기(Gradient)가 작아져서 생성자가 학습할 동력을 잃게 됩니다.

* **해결책:** 실제 구현에서는 $\\min\_G \\log(1 \- D(G(z)))$ 대신 \*\*$\\max\_G \\log D(G(z))$\*\*를 사용하여 학습 초기에도 강력한 기울기를 제공합니다.

### **이상적인 상태 (Nash Equilibrium)**

학습이 완벽하게 이루어지면 생성자가 만든 데이터의 분포($p\_g$)가 실제 데이터의 분포($p\_{data}$)와 일치하게 됩니다.

* 이때 판별자는 진짜와 가짜를 전혀 구분하지 못하고 확률 \*\*$1/2$\*\*로 찍게 되는 상태에 도달합니다.

## **4\. 요약 이미지 가이드**

문서 내 '그림 1'은 이러한 확률 분포의 변화를 보여줍니다.

1. **검은 점선 (**$p\_{data}$**):** 실제 데이터 분포  
2. **초록 실선 (**$p\_g$**):** 생성자가 만드는 데이터 분포  
3. **파란 점선 (**$D$**):** 판별자의 판별 경계

학습이 진행됨에 따라 초록색 실선이 검은색 점선과 겹쳐지게 되고, 파란색 점선은 모든 구간에서 $1/2$의 값을 갖는 수평선이 됩니다.
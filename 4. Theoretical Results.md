# **GAN (생성적 적대 신경망) 훈련 원리 및 이론적 결과**

GAN은 두 개의 신경망인 \*\*생성자(Generator, G)\*\*와 \*\*판별자(Discriminator, D)\*\*가 서로 대립하며 학습하는 독특한 구조를 가집니다. 본 보고서는 이들의 역할과 훈련 과정을 요약합니다.

## **1\. 구성 요소의 역할 및 목표**

GAN의 학습은 생성자와 판별자 사이의 '적대적 경연'으로 설명할 수 있습니다.

| 구분 | 생성자 (Generator, G) | 판별자 (Discriminator, D) |
| :---- | :---- | :---- |
| **역할** | 진짜 같은 가짜 데이터를 생성 | 데이터의 진위(진짜/가짜)를 판별 |
| **목표** | 판별자를 완벽히 속이는 것 | 진짜와 가짜를 정확히 구별하는 것 |
| **학습 방식** | 판별자가 '진짜'라고 판단할 확률을 극대화 | 진짜 데이터는 진짜로, 가짜 데이터는 가짜로 판단하도록 학습 |

## **2\. 이론적 목표 (Minimax Game)**

GAN의 목표는 다음의 가치 함수 $V(D, G)$를 이용한 **Minimax Game**으로 정의됩니다.

$$\\min\_{G} \\max\_{D} V(D, G) \= \\mathbb{E}\_{x \\sim p\_{data}(x)}\[\\log D(x)\] \+ \\mathbb{E}\_{z \\sim p\_{z}(z)}\[\\log(1 \- D(G(z)))\]$$

* \*\*판별자(D)\*\*는 $V(D, G)$를 최대화하려고 합니다 (진짜와 가짜를 더 잘 구분).  
* \*\*생성자(G)\*\*는 $V(D, G)$를 최소화하려고 합니다 (판별자가 가짜를 구분하지 못하게 함).

## **3\. 알고리즘의 핵심 단계**

훈련은 생성자와 판별자가 번갈아 가며 학습하는 방식으로 진행됩니다.

### **단계 1: 판별자(D) 훈련 (k번 반복)**

1. 노이즈 분포 $p\_g(z)$에서 미니배치 샘플 ${z^{(1)}, \\dots, z^{(m)}}$을 추출합니다.  
2. 실제 데이터 분포 $p\_{data}(x)$에서 미니배치 샘플 ${x^{(1)}, \\dots, x^{(m)}}$을 추출합니다.  
3. 판별자의 경사도(Gradient)를 계산하여 다음 식을 최대화하는 방향으로 $D$를 업데이트합니다.  
   $$\\nabla\_{\\theta\_d} \\frac{1}{m} \\sum\_{i=1}^{m} \[\\log D(x^{(i)}) \+ \\log(1 \- D(G(z^{(i)})))\]$$

### **단계 2: 생성자(G) 훈련 (1번 반복)**

1. 노이즈 분포 $p\_g(z)$에서 새로운 미니배치 샘플 ${z^{(1)}, \\dots, z^{(m)}}$을 추출합니다.  
2. 생성자의 경사도를 계산하여 다음 식을 최소화하는 방향으로 $G$를 업데이트합니다.  
   $$\\nabla\_{\\theta\_g} \\frac{1}{m} \\sum\_{i=1}^{m} \\log(1 \- D(G(z^{(i)})))$$

   (실제 구현에서는 $\\log D(G(z))$를 최대화하는 방식으로 학습하여 초기 학습 속도를 높이기도 합니다.)

## **4\. 결론 및 평형 상태**

학습이 이상적으로 진행되면, 생성자는 실제 데이터의 분포를 완벽하게 학습하게 됩니다($p\_g \= p\_{data}$). 이 시점에서 판별자는 진짜와 가짜를 더 이상 구분할 수 없게 되어 판별 확률이 $D(x) \= \\frac{1}{2}$이 되는 평형 상태에 도달합니다.
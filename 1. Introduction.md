# **GAN(Generative Adversarial Nets) 논문 서론 요약 **

## **1\. 연구 배경: 판별 모델의 성공과 생성 모델의 정체**

2014년 당시 딥러닝은 이미지나 음성 같은 고차원 데이터를 특정 카테고리로 분류하는 **판별 모델(Discriminative Models)** 분야에서 엄청난 성공을 거두고 있었습니다. 이러한 성공은 역전파(Backpropagation), 드롭아웃(Dropout), 그리고 ReLU와 같은 선형 유닛(piecewise linear units) 덕분이었습니다.

반면, 데이터를 새로 만들어내는 \*\*생성 모델(Generative Models)\*\*은 다음과 같은 이유로 큰 발전을 이루지 못하고 있었습니다.

* **복잡한 계산:** 최대 우도 추정(MLE) 과정에서 발생하는 수식들이 계산적으로 다루기 매우 어려웠습니다.  
* **알고리즘 활용의 어려움:** 판별 모델에서 효과적이었던 선형 유닛(ReLU 등)의 장점을 생성 모델에 접목하기가 까다로웠습니다.

## **2\. 제안하는 해결책: 적대적 프레임워크 (Adversarial Nets)**

이 논문은 위에서 언급한 어려움들을 우회하기 위해 \*\*"서로 경쟁하는 두 모델"\*\*을 동시에 학습시키는 새로운 프레임워크를 제안합니다.

### **🎯 핵심 모델 구성**

1. **생성자 (Generator, G):** 데이터의 분포를 학습하여 실제와 유사한 샘플을 생성합니다.  
2. **판별자 (Discriminator, D):** 입력받은 샘플이 실제 데이터인지, 아니면 G가 만든 가짜인지를 확률로 판별합니다.

## **3\. 직관적인 비유: 위조지폐범과 경찰**

논문에서는 이 경쟁 관계를 아주 유명한 비유로 설명합니다.

* **생성자(G) \= 위조지폐범:** 경찰을 속이기 위해 최대한 진짜 같은 가짜 지폐를 만들려고 노력합니다.  
* **판별자(D) \= 경찰:** 위조지폐와 진짜 지폐를 완벽하게 구별하려고 노력합니다.

이 두 팀이 경쟁하면서 서로의 기술을 발전시키다 보면, 결국 위조지폐범(G)은 경찰(D)이 도저히 구별할 수 없을 정도($D(x) \= 0.5$)의 완벽한 가짜를 만들게 된다는 것이 GAN의 핵심 원리입니다.

## **4\. GAN의 기술적 혁신 및 장점**

Introduction에서 강조하는 GAN의 차별점은 다음과 같습니다.

* **역전파만 사용:** G와 D가 모두 다층 퍼셉트론(MLP)으로 구성될 경우, 오직 역전파(Backprop)만으로 전체 시스템을 학습할 수 있습니다.  
* **단순함:** 복잡한 마르코프 체인(Markov chains)이나 근사 추론(Approximate inference)이 전혀 필요 없습니다.  
* **학습 효율:** 생성 과정에서 피드백 루프가 없기 때문에 판별 모델에서 성능이 검증된 다양한 알고리즘을 그대로 가져와 쓸 수 있습니다.

**요약하자면,** GAN의 서론은 "복잡한 수학적 계산 때문에 막혀있던 생성 모델의 한계를, 두 모델의 경쟁이라는 게임 이론적 관점으로 전환하여 단순하고 강력하게 해결했다"는 메시지를 전달하고 있습니다.